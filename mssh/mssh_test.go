package mssh

import(
	"testing"
)

func TestSSH(t *testing.T) {


	/*ssh := New(user)
	seesion,err := ssh.Connect()
	if err != nil {
		t.Error(err)
	}
	fmt.Printf("session:%#v\n",seesion)
	defer  seesion.Close()
	var sesionBuf bytes.Buffer
	seesion.Stdout = &sesionBuf
	err = seesion.Run("date")
	if err != nil {
		t.Error(err)
	}
	fmt.Println(seesion.Stdout)*/
}
